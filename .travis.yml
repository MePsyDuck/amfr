language: python

python:
- "3.6"

before_install:
- sudo apt-get update

install:
- pip install codecov
- pip install -r requirements.txt

virtualenv:
  system_site_package: true

script:
- coverage run -m unittest facial_recog/tests/test_app.

after_success:
- codecov
