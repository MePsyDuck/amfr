language: python

python:
- "3.6"

before_install:
- sudo apt-get update

install:
- pip install -r requirements.txt

virtualenv:
  system_site_package: true

before_script:
- cd ..

script:
  python -m unittest discover facial_recog "test_app.py"
